# seed-selector-plugin Specification

> **REMOVED** — 本规范对应的 `SeedSelectorPluginInterface` 与 `PluginType.SEED_SELECTOR` 已移除。
> 语义筛选合并为 TrimPlugin 实现：见 `plugin-protocol/spec.md` 中 "TrimPluginInterface" requirement，
> 以及 `mcs/plugins/trim/llm_seed_selector.py` 中的 `SemanticTrimPlugin`。

## Purpose
已移除：种子语义筛选功能由 TrimPlugin 链中的 `SemanticTrimPlugin`（opt-in）承担。

## Requirements

_无 requirement（接口与枚举值已移除）。_
