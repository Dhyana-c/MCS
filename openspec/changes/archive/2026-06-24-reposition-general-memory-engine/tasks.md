# Implementation Tasks

> 纯文档变更 + 1 处 spec delta（spec delta 已随本 change 生成）。架构 / 代码零改动。

## 1. README.md 改写

- [x] 1.1 `README.md:3` 定位句：删除"面向单一领域"，改为"完整的通用记忆引擎"定位（保留"不依赖 embedding / 向量检索"原句）。
- [x] 1.2 `README.md:7-11`「核心赌注」节 → 改名为「核心定位」：内容改为通用引擎定位 + 核心不变量指针；把"知识有足够的局部性……几跳语义游走"改述为"系统通过核心不变量 + 守门 + 聚类裂变**主动维持**语义局部性"。
- [x] 1.3 删除"对可教领域最成立 / 对跨领域、矛盾常态化知识发紧"的领域限定（诚实保留"以语义结构化记忆为长"，不夸大为万能）。

## 2. docs/architecture.md 改写

- [x] 2.1 `docs/architecture.md:13` 局部性"赌注"句改述为"系统保证"：核心不变量 + 守门 + 聚类裂变主动维持语义局部性，**不假设**外部知识天然就近。
- [x] 2.2 检查 `docs/architecture.md:9` 系统定位段是否含"单一领域 / 赌注"措辞，按需对齐"通用记忆引擎"口径（保守：仅改动确含旧措辞的句子）。

## 3. docs/faq.md 改写

- [x] 3.1 `docs/faq.md:7` 定位句：删除"面向单一领域"，对齐"通用记忆引擎"。
- [x] 3.2 `docs/faq.md:13-15`「"核心赌注"是什么意思？」FAQ：问题改名（如「系统的定位 / 局部性怎么来的？」），答案改述为"维持而非假设"。
- [x] 3.3 `docs/faq.md:51` 按 design.md D3 精确改写：替换"核心赌注是局部性"措辞为"LLM 直接读有界活跃双向视图"，**保留"默认不依赖向量兜底"结论**与"更准确"判断原样。

## 4. docs/INDEX.md 改写

- [x] 4.1 `docs/INDEX.md:9` README 导航描述：把"核心赌注"→"核心定位"。

## 5. 校验

- [x] 5.1 `openspec validate reposition-general-memory-engine` 通过。
- [x] 5.2 全文搜索 5 处文档：无"核心赌注 / 局部性假设 / 面向单一领域"残留（"局部性"作为"系统维持的属性"出现是预期的，"假设 / 赌注"措辞应为 0）。
- [x] 5.3 核对 doc-hierarchy spec「README 精简」Scenario：README 章节清单现含"核心定位"而非"核心赌注"。
- [x] 5.4 核对内部链接完整：`docs/INDEX.md`、`docs/faq.md`「进一步阅读」、README「文档」表链接目标存在。
