## 1. 估算 / 渲染口径统一（不变量地基）

- [x] 1.1 抽出单一"节点渲染"函数：`name==content` 去重（只写一份），`name!=content` 写两份（name + 含义）
- [x] 1.2 `_exceeds_budget` / `_select_batch` 改用该渲染量估算（含 name+content+去重），与 `context_renderer` 同口径
- [x] 1.3 单测：估算值 == 实际渲染 token 量（同口径断言）；`name==content` 去重生效、`name!=content` 计两份

## 2. decide_hub 多社区（一进多出）

- [x] 2.1 新增 `MultiHubDecision` schema：返回**多个社区**（每社区：主题 + 成员归属 + 重组策略）；旧单 `HubDecision` 已移除
- [x] 2.2 `decide_hub` prompt：输入中心点 + **全部**一跳子节点（去重渲染），要求输出多社区划分
- [x] 2.3 成员归属解析 + 校验：不重不漏、幻觉 id 过滤、无法分类者确定性兜底（留中心点下，不丢）
- [x] 2.4 输出规模控制：避免逐一回吐海量 id 撑爆输出（主题级 + 紧凑归属，或分批聚 + 按主题标签合并）

## 3. _reorganize / _select_batch 一进多出

- [x] 3.1 `_select_batch` → 取**全部**一跳子节点（不变量保证 ≤ 窗口；不再盲切固定批）
- [x] 3.2 `_reorganize` 支持一次造**多个** hub、按社区分挂（沿用 seed-graph-directional-hierarchy 的有向层级边）
- [x] 3.3 递归维持不变量：新 hub 邻域仍超则继续聚，直到图中处处一跳邻域 ≤ 容量
- [x] 3.4 单测：超容量节点一次归纳后直连数骤降、各 hub 一跳邻域 ≤ 容量
- [x] 3.5 重叠聚类：允许一个一跳子节点归属**多个** hub（去掉"恰好一个"硬划分），以总量下降为判据
- [x] 3.6 hub 复用（边吸收）：hub 生成后，一跳子节点 ⊇ 其全部成员的节点改连 hub（删直连边、加 X→hub）；单测覆盖超集吸收与"部分包含不吸收"
- [x] 3.7 重组总量校验：裂变 / 重叠 / 复用后断言图总 token / 节点 / 边未上升（净下降或为收敛所需）
- [x] 3.8 聚类三方式（知识重组）：`decide_hub` 每社区 = 合并同义 / 找关键概念 / 概括新概念之一；概括物做实质性校验（拒绝空洞聚合标签）。同步移除 / 弱化 `_is_overbroad_hub` 等事后补丁
- [x] 3.9 hub 同构渲染：`context_renderer` 对 `role=="hub"` 节点不加特殊标记，与普通概念同口径渲染

## 4. 验证与实跑

- [x] 4.1 全量 pytest 回归（默认 `seed_graph_bounding=False` 行为不变）
- [ ] 4.2 100 篇重建：root 度从 1735 摊薄、最大深度增加、**每节点一跳邻域 ≤ 容量**、无扁平超级根（需 LLM 环境）
- [x] 4.3 对比旧扁平图，确认 BREAKING 影响范围并文档标注需重建

## 5. 文档与宪法

- [x] 5.1 项目 `CLAUDE.md` 固化"最大上下文子图"宪法（不变量 + 算法原理 + 总体流程 + 两条铁律）
- [x] 5.2 更新 README / `subgraph-bounding` 说明：一进多出、口径对齐、旧图需重建
- [x] 5.3 归档旧扁平评测库说明
