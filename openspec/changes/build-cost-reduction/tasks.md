## 1. 成本 instrumentation（concrete 先行）

- [ ] 1.1 实现按 purpose/阶段的 token+调用计量（拦截 LLM 调用，归类 extract_concepts/judge_relations/阶段②查询/压缩）
- [ ] 1.2 汇总每块与整批成本，产出可比报告（JSON + 终端）
- [ ] 1.3 单测：mock LLM 下计量正确归类与求和

## 2. 真实 dry-run 成本模型

- [ ] 2.1 用实测数据标定建图 super-linear 模型（块数、图规模 → token）
- [ ] 2.2 修正 hotpot/multihop 的 dry-run，使预估含建图增长 + query 阶段
- [ ] 2.3 加运行时累计花费暴露 + 硬上限开关（达上限停止并报告）

## 3. 基线度量

- [ ] 3.1 在小规模 build（50–100 篇）上跑 instrumentation，记录各阶段占比，**实证"阶段②是不是最大头"**
- [ ] 3.2 记录基线（成本 + 连通性诊断），作为各优化的对照起点

## 4. 阶段②轻量化（最大杠杆，探索）

- [ ] 4.1 实现开关：阶段② 锚点定位可切换为轻量检索（先用 alias_index）
- [ ] 4.2 对照实验：开/关下的 token 成本 + 连通性诊断（孤立率/分量/跨文档边）
- [ ] 4.3 若 alias_index 不够，评估轻量本地嵌入方案与其自身成本
- [ ] 4.4 结论：是否纳入、用哪种；记录数据（强调"不得劣化连通性"）

## 5. 压缩延后（批量模式）

- [ ] 5.1 加 deferred/batch compaction 开关：bulk build 跳过每块压缩、结束统一跑一次
- [ ] 5.2 对照实验：省多少 token；与逐块压缩的最终图质量是否等价
- [ ] 5.3 结论与默认策略

## 6. 前缀缓存重排（探索）

- [ ] 6.1 重排各 purpose 的 prompt：固定内容前置、可变材料后置
- [ ] 6.2 实测缓存命中率变化与 input 成本节省
- [ ] 6.3 结论

## 7. judge_relations 嵌入预筛（探索，可选）

- [ ] 7.1 调研嵌入预筛方案与基础设施
- [ ] 7.2 小规模实验：精度/召回 + 省多少 LLM 调用
- [ ] 7.3 结论：是否值得

## 8. 综合评估

- [ ] 8.1 汇总各优化的"省 token vs 连通性影响"，给出纳入/不纳入清单
- [ ] 8.2 更新 dry-run 模型与文档；更新 `PENDING_FIXES.md`；与 graph-construction-quality 协调阶段②改动
