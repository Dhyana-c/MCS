## 1. token 估计修复（底座）

- [x] 1.1 `TokenBudget.estimate` 改为更准的估法（英文 ≈ `len/4` / 词数×1.3，CJK 近似 `len`），收敛为单一入口
- [x] 1.2 预留替换真分词器（如 tiktoken）的接口/hook，不实装、仅占位
- [x] 1.3 测试：英文不再 ~2× 高估、CJK 合理、空串=0

## 2. token-aware fanout 阈值

- [x] 2.1 fanout 触发改由「邻域渲染 token 累计 > 可用窗口」判定（替掉硬编码 `fanout_threshold=12`），给一个 floor（如 16）
- [x] 2.2 阈值随 `token_budget` / 上下文窗口配置变化
- [x] 2.3 测试：8k vs 16k 触发阈值不同；floor 生效；估计修复后能容纳的成员数符合预期

## 3. 中间概念节点真正落地（建图侧 `fanout_reducer`）

- [x] 3.1 `synthetic_hub_summary` → **真正新建**中间概念节点（`role="hub"`，content=归纳摘要），不再只记备注
- [x] 3.2 `hub_id` → 提拔现有节点为中间枢纽
- [x] 3.3 重组边（图手术）：把同组成员的连接收敛到中间节点之下，形成层次（星型）
- [x] 3.4 测试：构造超容量密集邻域，验证「建中间节点 + 边重组 + 成员挂到中间节点」

## 4. 虚拟根 + 种子图（查询侧，opt-in）

- [x] 4.1 阶段② 种子集视为一个虚拟根的子图；渲染量 > 容量时触发归纳（opt-in 开关，默认不改基线）
- [x] 4.2 递归 / 分层归纳：自底向上分批（每批 ≤ 阈值）`decide_hub` 归纳 → 递归直到根直接子集 ≤ 容量
- [x] 4.3 不超容量时透传种子；未启用时 `query()` 行为与现状一致
- [x] 4.4 测试：种子过多→分层；递归收敛；透传；opt-in 不变（构造已知种子集验证）

## 5. 验证（重建小图，测试机制）

- [ ] 5.1 重建一张小图（建图时启用中间概念归纳），产出带纵向层次的图
- [ ] 5.2 图诊断对照：最大连通分量占比↑ / 孤立率↓ / 出现中间概念层
- [ ] 5.3 启用查询种子图，看语义遍历是否展开（LLM/query↑、子图分层）+ candidate recall / recall 增量
- [ ] 5.4 小图实验扫参数（阈值 floor、递归深度），判断机制有效性

## 6. 文档

- [x] 6.1 更新文档：子图大小约束 / 中间概念归纳 / 虚拟根种子图机制、verify 流程、与 `graph-construction-quality` 的关系
