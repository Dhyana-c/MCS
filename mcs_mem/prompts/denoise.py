"""碎片去噪判定 prompt。

逐碎片判「值得记 / 噪声」，保守策略（拿不准就保留）。**只判去留、不改写 content**
（见 agent-consolidation spec「去噪不改写 content」）。
"""

DENOISE_PROMPT = """\
判断以下碎片是否值得记入知识图谱。值得记=包含事实、观点、计划、决策等有信息量的内容；噪声=纯闲聊、语气词、无实质内容的寒暄。

拿不准就保留（宁可多记不漏记）。

碎片：{content}

回答 ONLY "保留" 或 "丢弃"："""
