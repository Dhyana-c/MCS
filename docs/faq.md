# 常见问题（FAQ）

## 通用

### MCS 是什么？

MCS（Maximum-Context Subgraph）是一个完整的通用记忆引擎——由大模型语义驱动，把零散文本组织成图结构的语义记忆，并维持一条硬不变量使任意节点的活跃视图永远放得进一个 LLM 上下文窗口。不依赖 embedding / 向量检索。

### MCS 返回什么？

`query()` 默认返回一个 `Subgraph`（选中的节点 + 选中的 `关联` / `互斥` 边），不是自然语言答案。它专注于"记忆本身"，把合成答案、多轮对话等留给上层；后处理插件可把 `Subgraph` 转成其他形态（如自然语言字符串）。

### 系统的定位是什么？局部性从哪来？

MCS 是一个**完整的通用记忆引擎**。它**不假设**外部知识天然就近——而是靠核心不变量 + 守门 + 聚类裂变**主动维持语义局部性**：任意节点的活跃双向视图被收敛到语义内聚、渲染 token ≤ T，因此沿关联边的语义游走能在有界视野内到达相关知识。

## 使用

### 支持哪些 LLM 后端？

Phase 1 支持三种：
- **DeepSeek**：默认后端，OpenAI SDK 兼容
- **Claude / Anthropic**：可选，`pip install -e ".[claude]"`
- **Ollama**：本地推理，零 token 成本，需单独安装

### 需要 API key 吗？

可以不需要。`examples/` 下的示例默认走 mock 模式。使用 Ollama 本地后端也不需要 API key。使用 DeepSeek / Claude 后端需要对应的 API key。

### 如何切换 LLM 后端？

```python
# DeepSeek（默认）
mcs = create_mcs(llm="deepseek", db_path="mcs.db")

# Claude
mcs = create_mcs(llm="claude", db_path="mcs.db")

# Ollama（本地）
mcs = create_mcs(llm="ollama", db_path="mcs.db")
```

### 数据如何持久化？

MCS 默认开启自动落盘（`auto_persist=True`），使用 SQLite。每次 `ingest()` 完成后自动持久化。Builder 在 `build()` 时会自动从数据库加载已有数据。

## 设计

### 为什么不用向量检索？

MCS 默认不依赖向量相似度兜底——大模型直接阅读"装得下的有界活跃双向视图"做判断（关系发现、聚类、召回），靠核心不变量保证局部视野装得下、比向量相似度更准确（尤其在关系发现、聚类场景）。

### "最大上下文子图不变量"是什么？

任意节点的**活跃双向视图**（top-priority 的 {关系边 + 层级邻居}，截断后）渲染 token ≤ 一个上下文窗口 T。这里"关系边"即该节点的 `关联` / `互斥` 边，层级邻居是聚类涌现的下钻成员。这是导航、归纳、查询的共同地基——保证 LLM 永远能一次性读完任意节点的局部视野。注意有界的是**活跃 / 渲染视图**，不是存储（存储可保留低优先级长尾）。

### 边有哪些类型？

只有 2 类（登记制、谨慎增加）：

- **关联**：结构基础边（`source → target`），连接事实与端点、概念间关联、聚类形成的"组织中心 ↔ 成员"。**无 `kind` / `label`**——开放谓词落在**事实节点的 `content`**里（谓词落点）。一条只存一份，但两端邻接都索引到它（反查、双向可达）。
- **互斥**：当前唯一语义类型，表两条事实相互排斥（事实 ↔ 事实）。

没有独立的"层级"边——组织层级是聚类的产物，用 `关联` 边 + 中心节点的 `hub` 标记表达。

## 评测

### 评测指标是什么？

MultiHop-RAG 评测的指标为文档级检索：**Hit@k / Recall@k / MAP@k / MRR@k**。`query()` 返回的节点经 `source_tracking` 映射回来源文档，与 gold evidence 文档比对。

### 相关性重排为什么重要？

查询管线默认按 BFS 发现顺序返回（无排序），gold 文档常被埋没。开启查询侧词法重排（现为默认、零额外 LLM 调用）后，overall Hit@10 从 ~0.16 提升到 ~0.73。

## 进一步阅读

- [架构总览](architecture.md) — 系统定位、双层结构、插件体系
- [图模型设计](graph-model-design.md) — 完整、权威的图模型与核心算法设计
- [上手指南](getting-started.md) — 5 分钟跑通安装 / 写入 / 查询
